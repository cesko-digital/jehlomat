import { styled } from '@mui/system';

const Heading = styled('th')({
    fontStyle: 'normal',
    fontSize: 14,
    lineHeight: '24px',
    color: '#808285',
    textAlign: 'left',
    paddingRight: 12,
    position: 'relative',
});

export default Heading;
