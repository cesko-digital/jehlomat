import styled from "@emotion/styled";
import {grey} from "utils/colors";

export const Label = styled.label`
     {
        color: ${grey};
        display: block;
        padding-bottom: 5px;
        font-weight: bold;
        font-family: 'Roboto';
        font-size: 10pt;
    }
`;
