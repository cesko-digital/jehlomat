import styled from '@emotion/styled';
import { grey } from 'utils/colors';

export const Label = styled.label`
    color: ${grey};
    display: block;
    padding-bottom: 7px;
    font-family: 'Roboto';
    font-size: 10pt;
`;
